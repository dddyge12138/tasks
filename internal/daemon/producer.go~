// 需要在后台持续运行的协程
package daemon

import (
	"task/internal/model"
	"time"
)

func Init() {

}

func TaskProducerRun() {
	now := time.Now()
	var tasks []*model.Task
	c := time.Tick(30 * time.Second)
	for range c {

	}
}
